import { describe, it, expect, jest, beforeEach } from '@jest/globals';

describe('ache-cep-cli', () => {
  beforeEach(() => {
    jest.resetModules();
  });

  it('should return true', () => {
    expect(true).toBe(true);
  });
});
